#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):

    num1 = 0
    num2 = 
    length1 = len(tuple_a)
    length2 = len(tuple_b)
    if length1 == 0 and length2 == 0:
        num1 = 0
        num2 = 0
    if length1 == 1 and length2 == 1:
        num1 = tuple_a[0] + tuple_b[0]
        num2 = 0
    if length1 > 1 and :
        num2 = 0 + tuple_b[0]
    if length1 > 1:
        num1 = tuple_a[0] + tuple_a[1]
    if length2 > 1:
        num2 = tuple_b[0] + tuple_b[1]
    return (num1, num2)
        
